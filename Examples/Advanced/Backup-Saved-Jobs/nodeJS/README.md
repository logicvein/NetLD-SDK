# Node.js

Requires Node.js 20 or later and has no third-party dependencies.

```sh
cp .env.example .env
node backup-saved-jobs.mjs
```

For repository testing, use `node backup-saved-jobs.mjs --env ../../../.env.netld`.
Run tests with `npm test`.
