# Python

Requires Python 3.10 or later. Install `requirements.txt`, copy `.env.example` to `.env`, and run `python3 export_terminal_logs.py`.

Use `--env /path/to/file` to load another environment file. Run tests with `python3 -m unittest discover -s tests`.
