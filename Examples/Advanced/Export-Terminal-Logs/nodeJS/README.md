# Node.js

Requires Node.js 20 or later and has no third-party dependencies. Copy `.env.example` to `.env` and run `node export-terminal-logs.mjs`.

Use `--env /path/to/file` to load another environment file. Run tests with `node --test`.
