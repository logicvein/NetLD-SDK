# PowerShell

Requires PowerShell 7. Copy `.env.example` to `.env` and run `pwsh ./export-terminal-logs.ps1`.

Use `-EnvPath /path/to/file` to load another environment file. Run tests with `pwsh -NoProfile -File tests/Test-ExportTerminalLogs.ps1`.
