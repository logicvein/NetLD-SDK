# Node.js

Requires Node.js 20 or later and has no third-party dependencies. Copy `.env.example` to `.env` and run `node export-hardware-inventory.mjs`.

Use `--format json` or `--env /path/to/file` as needed. Run tests with `node --test`.
