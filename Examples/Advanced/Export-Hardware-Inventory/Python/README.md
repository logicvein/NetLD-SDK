# Python

Requires Python 3.10 or later. Install `requirements.txt`, copy `.env.example` to `.env`, and run `python3 export_hardware_inventory.py`.

Use `--format json` to override the configured output format or `--env /path/to/file` to use another environment file. Run tests with `python3 -m unittest discover -s tests`.
