# PowerShell

Requires PowerShell 7. Copy `.env.example` to `.env` and run `pwsh ./export-hardware-inventory.ps1`.

Use `-Format json` or `-EnvPath /path/to/file` as needed. Run tests with `pwsh -NoProfile -File tests/Test-ExportHardwareInventory.ps1`.
