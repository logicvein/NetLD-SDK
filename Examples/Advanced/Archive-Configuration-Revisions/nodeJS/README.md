# Node.js

Requires Node.js 18 or later. Copy `.env.example` to `.env`, set the connection values, and run:

```console
node archive-configuration-revisions.mjs
```

Use `node archive-configuration-revisions.mjs --env /path/to/file` to load another environment file. Run the offline tests with `node --test`.
