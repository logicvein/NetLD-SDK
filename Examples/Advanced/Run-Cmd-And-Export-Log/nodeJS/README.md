# Node.js implementation

```shell
npm install
cp .env.example .env
cp commands.txt.example commands.txt
node run-cmd-and-export-log.mjs
```

Set `NETLD_RUN_JOB=true` only after reviewing the preview. Run tests with:

```shell
npm test
```

